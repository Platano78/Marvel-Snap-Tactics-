# EOS Audit — Snapapoulous Prime (2026-07-19)

**Method**: execution-os `audit-a-project` playbook under /crew discipline.
**Baseline**: `8d53711` (ICM router committed pre-audit).
**Crew**: 6 sonnet finder agents (one per dimension) + 6 haiku adversarial verifiers + orchestrator first-hand re-probes.
**Prior rulings honored**: ADR-001 (single `index.html`, CDN-only, no build step) — no row proposes a bundler, modules, or `node_modules`. ADR-002 (OAuth primary), ADR-003 (embedded card data) — no row proposes an API-required data path.

## Verdict key

- **CONFIRMED** — finder claim survived haiku adversarial verification (and orchestrator re-probe where noted).
- **RESTRUCTURED** — claim was directionally right but wrong in substance; row rewritten to ground truth.
- **rejected** — claim refuted; retained for the record.

## Master table (ranked by impact)

| # | System | Finding | Evidence | Verdict | Rank |
|---|--------|---------|----------|---------|------|
| 1 | PWA | SW never caches the CDN runtime (React/ReactDOM/Babel/Tailwind); fetch handler bails on all cross-origin at `sw.js:218` before any caching → true offline cold boot renders a blank page; only the evictable browser HTTP cache masks it | `sw.js:218` `if (url.origin !== location.origin) return;`; `index.html:94,97-99` CDN script tags; precache list `sw.js:9-18` has no CDN URLs. Verified by haiku + orchestrator re-probe | CONFIRMED | HIGH |
| 2 | Data | `CARD_DATA_FALLBACK` (57 cards, all labeled series:1, `index.html:1660-1720`) diverges from authoritative `card-data.json` (433 cards): only 10/57 are actually series:1; costs/powers/abilities diverge too (fallback Human Torch cost:1/series:1 vs real cost:3/series:6) | Verified by independent JSON parse in both finder and verifier | CONFIRMED | HIGH |
| 3 | Data | Fallback spells "Ant-Man" (hyphen); card-data.json has "Ant Man" — exact-name lookups silently fail across the fallback/authoritative boundary | `index.html:1662` vs jq search of card-data.json | CONFIRMED | HIGH |
| 4 | Data | AI advisor meta context + spotlight schedule are ~4.5 months stale: `meta-context.json` lastUpdated "March 2026"; `spotlight-schedule.json` rows end 2026-03-24 — nothing covers today | `data/meta-context.json:2`, `data/spotlight-schedule.json:2`; feeds AI system prompt (`index.html:1958`) and Oracle tab (`index.html:7462`) | CONFIRMED | HIGH |
| 5 | Data/PWA | "Refresh Game Data" button is a silent no-op for spotlight data: page requests `{cache:'reload'}` but SW's SWR whitelist (`sw.js:255`) covers only card-data/persona/meta-context; spotlight falls to cache-first handler which ignores the reload hint; button still reports "Updated" | `index.html:9187-9191`, `sw.js:255,266+` | CONFIRMED | MED |
| 6 | AI | Gemini OAuth (the ADR-002 PRIMARY path) requests scope `generative-language.retriever` (`index.html:1513`) and sends that token to `generateContent` (`index.html:2540-2550`). Code facts verified; whether the scope authorizes inference needs a live sign-in test — if not, every new "Continue with Google" user silently 403s on first message | Verified code path: `initTokenClient` → localStorage token → `callGeminiOAuth` Bearer header | CONFIRMED (code facts; semantic break needs live probe) | HIGH |
| 7 | AI | Groq model list ships retired `mixtral-8x7b-32768` (3rd dropdown entry → 4xx); Claude `defaultModel` is 14-month-old `claude-sonnet-4-20250514` (new users get the stale default) | `index.html:2241`, `index.html:2249-2250` | CONFIRMED | MED |
| 8 | Storage | RESTRUCTURED from "user's RapidAPI key lost on reload": the feature is entirely vestigial — exactly ONE occurrence of `rapidapi` in index.html (`8191`, a `useState` read); `setRapidApiKey` has zero call sites, so no input is even wired. Dead state line, not data loss | Orchestrator re-probe: `grep -in rapidapi index.html` → 1 line. Finder claimed HIGH data-loss bug; ground truth is a dead feature stub | RESTRUCTURED | MED→LOW |
| 9 | Storage | Write-only ghost keys: `snap_battlepass`, `snap_missions`, `snap_mastery` written every folder sync (`index.html:3476,3485,3506`), never read by any UI | Zero read call sites per finder grep | CONFIRMED | MED |
| 10 | Monolith | Dead component `CardPerformanceView` — 202 lines (`index.html:7984-8185`) with own state/effects, zero render sites, no route | `grep -c "<CardPerformanceView"` = 0 | CONFIRMED | HIGH (weight) |
| 11 | Monolith | Nested component definitions: `StatusBadge` inside `Settings` (`index.html:8682`), `DeckSelector` inside `DeckComparison` (`~9690`) — new identity every render, remount churn | Finder line refs | CONFIRMED | MED |
| 12 | Monolith | Win-rate/cube math implemented 4×: canonical `getMatchStats` (`1929`) plus reimplementations at `2124` and twice in VaultManager (`3602`, `3623`) | Finder line refs | CONFIRMED | MED |
| 13 | Monolith | `TAB_INDEX` (`3940`) missing `database`/`compare` → wrong slide animation direction on those tabs (cosmetic) | Only consumer `PageTransition` has `?? 0` fallback | CONFIRMED | LOW |
| 14 | Monolith | Dead minor components: `PullToRefreshIndicator` (`1470`), `SkeletonGrid` (`3931`) — def-only, zero uses | Finder grep counts | CONFIRMED | LOW |
| 15 | Docs | ADR-004 (Marvel red/blue/gold) fully superseded by shipped Cosmic Purple palette; no ADR-005 records the pivot | `index.html:118-123` cosmic tokens live; `#ED1D24` survives only as skip-link focus (`index.html:1356`); manifest colors cosmic; `grep -c ADR-005` = 0 | CONFIRMED | HIGH (doc truth) |
| 16 | Docs | Project memory system dormant since 2026-01-23: bugs.md last entry BUG-003 (Jan 23), issues.md stops at PR #25/#26, key_facts.md says "Season Dragons (January 2026)" — misses Cosmic Purple redesign, Stitch port, 3 named bug-fix commits | Orchestrator re-probe of all three files | CONFIRMED | HIGH (doc truth) |
| 17 | Docs | key_facts.md storage-key list wrong both directions: lists 3 keys that don't exist (`snap_ai_provider`, `snap_api_keys`, `snap_google_token`), missing ~13 real keys; real config lives in `snap_ai_config` + `snap_google_access_token`/`_expiry` | `key_facts.md:52-54` vs verified 21-key inventory | CONFIRMED | MED |
| 18 | Docs | No .gitignore; 5.9MB untracked junk (3 root screenshots + `docs/stitch_snapapoulous_prime_dashboard.zip`); zip duplicates already-tracked `docs/stitch_designs/…` content | Verified incl. zip-content overlap check | CONFIRMED | MED |
| 19 | Docs | CLAUDE.md legacy spec drift: RapidAPI subsystem spec'd 35× but ~unimplemented (1 code ref); `cardData.js` never existed (shipped as `card-data.json`); stale model IDs; file-structure tree omits `data/` + `card-data.json` (README structure section has the same gap) | grep counts + find | CONFIRMED | MED |
| 20 | Data | `data/learning/learning-state.json` (6.7KB) is a complete orphan — zero references in index.html or sw.js | grep "learning" → 0 matches | CONFIRMED | LOW |
| 21 | PWA | `spotlight-schedule.json` not in SW precache (meta-context.json is) → feature data missing offline until first online visit to that view | `sw.js:9-18` | CONFIRMED | MED |
| 22 | PWA | Manifest `theme_color` `#1c1022` vs meta theme-color `#120818` (both cosmic; minor inconsistency); CDN scripts unpinned (Babel, Tailwind) | `manifest.json:9`, `index.html:6-8,94,99` | CONFIRMED | LOW |
| 23 | AI | Settings "Test Connection" swallows the real error (`catch` sets only a status flag, UI shows bare "Failed"); chat path correctly surfaces `err.message` | `index.html:8336-8339` vs `5352-5354` | CONFIRMED | LOW-MED |
| 24 | Memory (meta) | Session auto-memory corrections needed: (a) `snap_shop`/`snap_conquest`/`snap_play_state` were REMOVED from code ("Economy HQ trimmed", `index.html:3502`) after the Feb 28 memory was written; (b) "URL whitelist 7/15 tabs" debt is FIXED — whitelist now covers all 10 live routes (`index.html:10198` vs routes `10377-10425`) | Finder greps; whitelist verified | CONFIRMED | LOW (correction) |

## Health positives (verified, no action)

- Section-banner anchors in AGENTS.md: **zero drift** across all 32 banners — router fully trustworthy.
- Card-art URL helper clean post-29ad17d; all 20 remaining `defId` refs are the correct import-resolution use case.
- Persona injection correct on all 6 provider paths (native `systemInstruction` for Gemini, top-level `system` for Claude).
- `anthropic-dangerous-direct-browser-access` header present — Claude provider is not CORS-blocked.
- Export sanitizes secrets (`apiKeys: {}`, `googleToken: null`); no key logging anywhere.
- SW cache-version discipline sound (monotonic CACHE_NAME bumps, activate cleanup, SW_UPDATED reload).
- IndexedDB used correctly and minimally (directory handle + sync timestamp only); no quota risk found in localStorage.
- Google OAuth client_id is real (not a placeholder); GIS flow fully wired.

## Buckets

### Do-now (verified defect or measured waste — apply this pass)
1. **sw.js: runtime-cache the CDN origins** (unpkg, cdn.tailwindcss.com) — closes row 1; honors CDN-only invariant (caches responses, adds no build step). Also add `spotlight-schedule.json` to precache + SWR whitelist (rows 5, 21) and bump CACHE_NAME.
2. **Regenerate `CARD_DATA_FALLBACK` from card-data.json** (script the 57→verified subset; fixes rows 2, 3 including Ant-Man spelling).
3. **Model-list refresh**: drop `mixtral-8x7b-32768`, bump Claude default off the 2025 snapshot (row 7). Fix Gemini OAuth scope to the documented generateContent scope (row 6) — gated on owner live sign-in test.
4. **Dead-code removal**: `CardPerformanceView`, `PullToRefreshIndicator`, `SkeletonGrid`, dead `rapidApiKey` state line (~230 lines; rows 8, 10, 14) — owner confirms at triage since deletion.
5. **Hoist nested components** `StatusBadge`/`DeckSelector` (row 11); add `database`/`compare` to `TAB_INDEX` (row 13).
6. **Consolidate stat math** onto `getMatchStats` (row 12).
7. **Docs truth pass**: write ADR-005 (Cosmic Purple supersedes ADR-004); backfill bugs.md/issues.md/key_facts.md for Feb–Apr work; fix key_facts key list; README + CLAUDE.md structure-tree corrections (rows 15, 16, 17, 19).
8. **Add .gitignore** (`Screenshot*.png` root, `docs/*.zip`, `*.Zone.Identifier`) (row 18a).
9. **Surface err.message in Test Connection** (row 23); align theme_color values; pin CDN versions (row 22).
10. **Correct session auto-memory** (row 24: removed snap_shop keys; whitelist debt fixed).

### Optional build (needs a design session, not a diff)
- **Meta/spotlight content refresh** (row 4): needs current-season research (what is the July 2026 meta), then a data update + a sustainable refresh cadence. The SW plumbing fix is do-now; the content is a research task.
- **Ghost-key features** (row 9): battlepass/missions/mastery data is parsed and stored every sync — either build the UI surfaces that display them or stop parsing. Product call + design.
- **RapidAPI feature** (row 8): implement per CLAUDE.md spec or formally descope it from the spec. Currently a one-line stub.

### Deferred ruling (owner's call)
- **Delete the 5.9MB junk** (3 screenshots + duplicate zip) — recommendation: delete zip (content is tracked extracted), ask on screenshots (row 18b).
- **`data/learning/learning-state.json`** — keep (future feature) or delete (orphan)? Recommendation: delete or move under docs/ as archival (row 20).
- **`docs/mockups/` standalone dashboards** — likely superseded by shipped Cosmic Purple; not verified stale. Recommendation: leave until checked.
- **CLAUDE.md legacy spec** — trim the unshipped RapidAPI section entirely vs annotate as aspirational. Recommendation: annotate, don't delete (it's Layer-3 reference).

## Outcomes (annotate as applied)

- **Owner triage (2026-07-19)**: apply ALL do-now; delete junk (both zip + screenshots — done, scratchpad-backed); KEEP `data/learning/learning-state.json`.
- **Rows 1, 5, 21, 22 — DONE `553e2e4`** (slice A): SW caches CDN runtime into stable `snapapoulous-cdn-runtime` cache; spotlight-schedule.json precached + SWR-whitelisted; CACHE_NAME v4; tailwind/babel pinned; theme_color aligned. Live-proven: offline cold reload renders fully.
- **Row 25 (NEW, found during apply): qrcode CDN 404** — pinned `qrcode@1.5.3/build/qrcode.min.js` 404s (1.5.x dropped the prebuilt bundle; agent-proposed 1.5.4 also 404s). Fixed to `1.4.4` (last standalone build, verified 200). QR vault sync was broken in prod until this. DONE `553e2e4`.
- **Row 18 — junk deleted** (5.9MB), .gitignore still pending in docs slice.
- **Row 24 — DONE**: session auto-memory corrected (snap_shop/conquest/play_state removal; whitelist debt closed).
- **Direction ruling (frontend-design-pipeline)**: Cosmic Purple committed as the sole world (`marvel_snap_mobile` Stitch project = authority); comic remnants purge underway — dashboard slice first with owner review gate, then sweep.
- **Dashboard slice — DONE `172da7f`**: Cosmic Purple commit for dashboard + shared shell (comic purge, Stitch stat cards, pill quick-match, card-art empty state, install-banner overlap fix). Three-track verified. Sweep gated on owner render review.
- **Feature-restore rulings (owner, 2026-07-19)**: RESTORE Analytics tab (wire existing CardPerformanceView — supersedes row 10's delete ruling), Battle Pass/Missions/Mastery surfaces (over live ghost keys, supersedes row 9), Hall of Armor (fresh UI over parsed variants), Deck Simulator (full rebuild from git-history spec — own arc after sweep). Economy HQ STAYS DEAD (Mar 1 trim ruling honored). `data/learning/learning-state.json` re-ruled DELETE (it's MKG router telemetry) — DONE `30633c1`, supersedes row 20's keep.
- **Owner ruling (2026-07-19): MKG Bridge is OUT of scope for this project** — it is the owner's personal dev MCP infrastructure, not a product feature. The AI audit's "MKG provider spec'd but unimplemented" finding resolves as: code is correct as-is; strip MKG Bridge from CLAUDE.md's provider spec/architecture diagram in the docs truth pass. Never implement an MKG provider here.
